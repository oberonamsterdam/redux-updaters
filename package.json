{
  "name": "oberon-redux",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "test": "npm run build && jest",
    "build": "npm run build:clean && npm run build:lib && npm run build:docs",
    "build:clean": "rimraf lib/",
    "build:lib": "babel src --out-dir lib",
    "build:docs": "documentation build src/index.js -f md > api.md"
  },
  "author": "Richard van Willegen",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-eslint": "^8.2.1",
    "babel-preset-env": "^1.6.1",
    "babel-preset-flow": "^6.23.0",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "eslint": "^4.16.0",
    "eslint-config-oberon": "^1.1.3",
    "flow-bin": "^0.64.0",
    "jest": "^22.1.4",
    "rimraf": "^2.6.2"
  },
  "PeerDependencies": {
    "redux": "^3.0.0",
    "redux-thunk": "^2.0.0"
  },
  "eslintConfig": {
    "extends": "oberon"
  },
  "dependencies": {
    "dot-prop": "^4.2.0",
    "snake-case": "^2.1.0",
    "typeof-is": "^1.0.2"
  }
}
